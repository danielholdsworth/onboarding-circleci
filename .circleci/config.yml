version: 2.1
 
orbs:
  my-orb:
    jobs:
      my_job:
        docker:
          - image: cimg/base:2021.04
        steps:
          - run: 
              command: | 
                curl -fsSL -o install-kconnect.sh https://raw.githubusercontent.com/fidelity/kconnect/main/scripts/install-kconnect.sh
                chmod 700 install-kconnect.sh
                ./install-kconnect.sh
                cd kconnect && ls -a
                cat $HOME/.kconnect/config.yaml
                ./kconnect help
          - persist_to_workspace:
              root: .
              paths:
                - .

jobs:
  build:
    docker:
      - image: alpine
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Echo 
          command: | 
            echo hello world

            ls -a

            cd kconnect

            ./kconnect help

workflows:
  main:
    jobs:
      - my-orb/my_job
      - build:
          requires: 
            - my-orb/my_job