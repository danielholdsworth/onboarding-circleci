version: 2.1

workflows:
  build-docker-image-only:
    jobs:
      - echo-commands

jobs:
  echo-commands:
    docker:
      - image: circleci/node:12.9.1-browsers
    steps:
      - run: echo "${CIRCLE_BUILD_NUM}"
          
          